[build]
  command = "npm run build"
  publish = ".output/public"

# TanStack Start/Nitro generates the server function in .netlify/functions-internal.
# Keep provider secrets in Netlify's environment-variable settings, never in Git.
